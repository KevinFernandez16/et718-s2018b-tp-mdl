<%= form_with(model: favorite, local: true) do |form| %>
  <% if favorite.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(favorite.errors.count, "error") %> prohibited this favorite from being saved:</h2>

      <ul>
      <% favorite.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Players %>
    <%= form.text_field :Players, id: :favorite_Players %>
  </div>

  <div class="field">
    <%= form.label :Name %>
    <%= form.text_field :Name, id: :favorite_Name %>
  </div>

  <div class="field">
    <%= form.label :Club %>
    <%= form.text_field :Club, id: :favorite_Club %>
  </div>

  <div class="field">
    <%= form.label :Nationality %>
    <%= form.text_field :Nationality, id: :favorite_Nationality %>
  </div>

  <div class="field">
    <%= form.label :Position %>
    <%= form.text_field :Position, id: :favorite_Position %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
